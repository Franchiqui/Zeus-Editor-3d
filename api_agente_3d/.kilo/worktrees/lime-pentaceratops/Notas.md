`npm run electron:build:win`

`node zeus-bridge.js`


Para que funcione
Reiniciar el Electron Bridge (para que cargue los nuevos endpoints)
Reiniciar la app Next.js con 
`npm run dev:main`
Abre el editor → el chat verá 
`hasActionExecutor = true` automáticamente